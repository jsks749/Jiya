<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Happy Birthday Jiya 🎂</title>
<style>
body {
  margin: 0;
  text-align: center;
  font-family: 'Poppins', sans-serif;
  background: linear-gradient(135deg, #ff9a9e, #fad0c4);
  overflow: hidden;
  color: #fff;
}
h1 {
  margin-top: 15px;
  font-size: 2.2em;
  animation: glow 2s infinite alternate;
}
@keyframes glow {
  from { text-shadow: 0 0 10px #fff, 0 0 20px #ff69b4; }
  to { text-shadow: 0 0 20px #fff, 0 0 40px #ff1493; }
}
img {
  width: 70%;
  max-width: 300px;
  border-radius: 20px;
  box-shadow: 0 0 25px rgba(255,255,255,0.5);
  margin-top: 15px;
}
p {
  font-size: 1.1em;
  padding: 15px;
}
button {
  background: white;
  color: #ff1493;
  border: none;
  padding: 12px 25px;
  border-radius: 30px;
  font-weight: bold;
  cursor: pointer;
  margin-top: 15px;
}
button:hover {
  transform: scale(1.1);
}
canvas {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  pointer-events: none;
}
</style>
</head>
<body>

<canvas id="confetti"></canvas>

<h1>🎂 Happy Birthday, Jiya! 🎉</h1>

<!-- her photo -->
<img src="data:image/jpeg;base64,/9j/4AAQSkZJRgABAQAAAQABAAD..." alt="Jiya's Photo">

<p>Born on <b>30th October 2006</b> 💖<br>
You make every heart smile! 🌸<br>
Tap below for a little surprise 🎁</p>

<button id="startBtn">Tap to Start 🎈</button>

<audio id="song" src="https://cdn.pixabay.com/download/audio/2022/03/15/audio_b8cf92e4f1.mp3?filename=happy-birthday-113985.mp3"></audio>

<script>
// Confetti
function startConfetti() {
  const canvas = document.getElementById("confetti");
  const ctx = canvas.getContext("2d");
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  const confetti = Array.from({length: 120}).map(() => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height - canvas.height,
    r: Math.random() * 6 + 2,
    d: Math.random() * 2 + 1,
    color: `hsl(${Math.random()*360}, 100%, 70%)`
  }));

  function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    confetti.forEach(p => {
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = p.color;
      ctx.fill();
    });
    confetti.forEach(p => {
      p.y += p.d;
      if (p.y > canvas.height) {
        p.y = -10;
        p.x = Math.random() * canvas.width;
      }
    });
  }
  setInterval(draw, 20);
}

// Tap button
document.getElementById("startBtn").addEventListener("click", () => {
  document.getElementById("song").play();
  startConfetti();
  alert("🎉 Happy Birthday, Jiya! Stay amazing always 💕");
});
</script>

</body>
</html>